{
  "name": "actioncode-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ActionCode - Telegram to GitHub Actions bridge",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
